data(
    deseq,
    package = "DESeqAnalysis",
    envir = environment()
)
data(
    matrix_lfc,
    package = "acidtest",
    envir = environment()
)
data(
    gsea,
    package = "pfgsea",
    envir = environment()
)
